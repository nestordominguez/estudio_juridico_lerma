<%= tinymce_assets %>
<%= form_for(@admin) do |f| %>
  <% if @admin.errors.any? %>
    <div id="error_explanation">
    <% if @admins.errors.count == 1 %>
      <h2> 1 error prohibe que estos datos sean guardados:</h2>
      <%else%>
      <h2> <%= @admins.errors.count %> errores prohiben que estos datos sean guardados:</h2>
      <% end %>
      <ul>
      <% @admins.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :abogado, "Tílde la casilla para seleccionar"%><br>
    <%= f.check_box :abogado %>
  </div>
  <div class="actions">
    <%= f.submit "Actualizar", :class => 'btn btn-sample btn-sm' %>
    <%= tinymce %>
  </div>
<% end %>
