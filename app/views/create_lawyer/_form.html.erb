<%= tinymce_assets %>
<%= form_for(@admin) do |f| %>
  <div class="field">
    <%= f.label :abogado, "Tílde la casilla para seleccionar"%><br>
    <%= f.check_box :abogado %>
  </div>
  <div class="actions">
    <%= f.submit "Actualizar", :class => 'btn btn-sample btn-sm' %>
    <%= tinymce %>
  </div>
<% end %>
